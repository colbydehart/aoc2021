defmodule Advent.DayOneTest do
  use ExUnit.Case
  doctest Advent.DayOne

  # :code.priv_dir(:advent)
  # |> Path.join("./day_one_input_1.txt")
  # |> File.read!()
  # |> String.split()
  # |> Enum.map(&Integer.parse/1)
  # |> Enum.map(&elem(&1, 0))
  # |> Advent.DayOne.part_one()
  # >> 1681
end
