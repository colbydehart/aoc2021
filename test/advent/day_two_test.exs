defmodule Advent.DayTwoTest do
  use ExUnit.Case, async: true
  doctest Advent.DayTwo

  # :code.priv_dir(:advent)
  # |> Path.join("./day_two_input.txt")
  # |> File.read!()
  # |> String.split("\n")
  # |> Enum.filter(& &1 !== "")
  # |> Advent.DayTwo.part_one()
  
  # :code.priv_dir(:advent)
  # |> Path.join("./day_two_input.txt")
  # |> File.read!()
  # |> String.split("\n")
  # |> Enum.filter(& &1 !== "")
  # |> Advent.DayTwo.part_two()
end
