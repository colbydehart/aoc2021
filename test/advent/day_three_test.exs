defmodule Advent.DayThreeTest do
  use ExUnit.Case, async: true
  doctest Advent.DayThree

  # :code.priv_dir(:advent)
  # |> Path.join("./day_three_input.txt")
  # |> File.read!()
  # |> String.split("\n")
  # |> Enum.filter(& &1 !== "")
  # |> Advent.DayThree.part_one()

  # :code.priv_dir(:advent)
  # |> Path.join("./day_three_input.txt")
  # |> File.read!()
  # |> String.split("\n")
  # |> Enum.filter(& &1 !== "")
  # |> Advent.DayThree.part_two()
end
